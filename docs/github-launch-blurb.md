# GitHub launch blurb

`feishu-flow-kit` is a local-first starter repo for practical Feishu automations: it already runs a small webhook flow locally, supports `/todo` and `/doc` command demos, and can optionally create a Feishu doc with starter heading / bullet / todo / paragraph blocks so the repo is useful before any heavy deployment work.
